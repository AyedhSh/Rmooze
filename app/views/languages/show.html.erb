<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @language.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @language.description %>
</p>

<%= link_to 'Edit', edit_language_path(@language) %> |
<%= link_to 'Back', languages_path %>

<%@language.lessons.each do |lesson| %>

<p>
  <strong>Name:</strong>
  <%= lesson.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= lesson.description %>
</p>
<p><%= lesson.language.id%></p>

<%= link_to 'Edit', edit_lesson_path(language_id: @language.id, id:lesson.id) %> 
<%end%>
